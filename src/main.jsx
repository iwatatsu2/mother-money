import React from 'react';
import ReactDOM from 'react-dom/client';
import MoneyGame from '../MoneyGame';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MoneyGame />
  </React.StrictMode>
);
